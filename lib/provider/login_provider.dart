import 'package:flutter/material.dart';

class LoginProvider extends ChangeNotifier{

  // Future<String>userLogin(String phone, String email, String bound, String pass) async {
  //   return await LoginApi.fetchLogin(phone, email, bound, pass);
  // }

}